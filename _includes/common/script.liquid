<script>
    window.ui = {
        title: "{{ site.title }}",
        baseurl: "{{ site.baseurl }}",
        i18n: {
            search_results: "{{ __.search_results | default: '검색결과' }}",
            search_results_found: "{{ __.search_results_found | default: '검색완료, # 의 페이지에서 검색결과가 나왔습니다.' }}",
            search_results_not_found: "{{ __.search_results_not_found | default: '검색결과 없음!' }}"
        }
    };
</script>
